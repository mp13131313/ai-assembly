# Secrets
.env

# macOS
.DS_Store

# Python
venv/
**/__pycache__/
*.pyc
*.pyo

# Run logs
*.log

# IDE
.vscode/
.idea/

# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# Scratch — experiments, one-offs, anything disposable
scratch/

# Audio files (large, not version-controlled anywhere in the tree)
*.m4a
*.mp3
*.wav
*.mp4
*.mov
*.webm

# Cached primary text fetches that contain large copyrighted material
# (runtime side — the personas pipeline's runs/ now lives under PROJECT_ROOT,
# outside this repo; see _workspace/planning/OPEN_ITEMS.md Tier 3)
runs/*/01_research/primary_texts/raw/
_workspace/planning/RUNTIME_REVIEW_2026_04_20.md

# Sentinel-regen baselines (per-prompt-edit snapshots used for diff tooling)
_workspace/sentinel_baselines/

# arch-03 baseline snapshots (Phase L pre/post snapshots; local-only dev
# artifacts kept for backward comparison, not tracked in git per FU#11
# post-Phase-L cleanup 2026-04-27)
_workspace/arch_03_baseline_snapshot/
