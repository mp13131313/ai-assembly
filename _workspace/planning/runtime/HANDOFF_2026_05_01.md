# Runtime — handoff 2026-05-01 (session-state-of-the-moment)

**Branch:** `voice-pipeline-v2.1-align-revert` (20+ commits ahead of `main`, pushed)
**Code repo HEAD:** `9eb0222` docs(planning): FU#61 v3 LANDED (persona thread)
**Athens-2026 data repo HEAD:** `5088d67` voice(dostoevsky): land FU#61-fresh quality_criteria
**Tests:** 223/223 passing
**Days to Athens:** 6 (Athens Day 1 evening = May 7 2026)

---

## TL;DR

Two threads ran in parallel today on the shared branch. Persona thread landed FU#61 v3 (Pass 4b prompt addition + Cleopatra + Dostoevsky card patches); runtime thread (this one) consolidated a comprehensive design + planning audit. Net deliverables this session for runtime:

1. **`_workspace/planning/runtime/` subfolder created** — authoritative truth going forward for runtime work
2. **`runtime/OPEN_ITEMS.md`** (526 lines) — all runtime-relevant items consolidated from this thread + 15/15 planning-folder docs read line-by-line
3. **`runtime/ONBOARDING.md`** (409 lines) — durable runtime-specific fresh-session pickup
4. **`runtime/HANDOFF_2026_05_01.md`** (this doc) — session state
5. **FU#62 filed** in FOLLOW_UPS.md (validation regen-on-flag is unimplemented)
6. **Step 3 redesign question surfaced + reasoned through** — operator preferred B+ shape (metadata + optional postscript) but no decision locked
7. **Editor / Frame layer architectural reasoning landed** as `_workspace/planning/PIPELINE_DOWNSTREAM_DESIGN_2026_04_30.md` (untracked; commit pending)

**Step 3 redesign + editor layer + microsite + broadsheet + Substack draft pass are the load-bearing Athens-blockers** beyond the runtime modules already built. None are decided yet.

---

## What this session landed

### Runtime planning consolidation (this thread)

| Deliverable | Status |
|---|---|
| Runtime subfolder created | `_workspace/planning/runtime/` |
| `runtime/OPEN_ITEMS.md` written | 526 lines, authoritative |
| `runtime/ONBOARDING.md` written | 409 lines, durable scaffolding |
| `runtime/HANDOFF_2026_05_01.md` written | this doc |
| Coverage audit | All 15 planning-folder docs read line-by-line in full this session; cross-referenced against this thread |
| FU#62 filed | In FOLLOW_UPS.md HEAD (validation regen-on-flag gap) |

### Pipeline architectural design (this thread)

| Deliverable | Status |
|---|---|
| `_workspace/planning/PIPELINE_DOWNSTREAM_DESIGN_2026_04_30.md` (354 lines) | Written; **uncommitted** |
| `_workspace/planning/STEP3_REDESIGN_2026_04_30.md` (362 lines) | Written; **uncommitted; STALE on shape** (assumes C-shape; B+ now recommended) |
| `_workspace/planning/FU61_DRYRUN_VERDICT_2026_04_30.md` (113 lines) | Written; **uncommitted; superseded** by FOLLOW_UPS.md FU#61 v1+v3 entries |

### FU#61 v3 cycle (cross-thread)

Persona thread + runtime thread coordinated on FU#61 cold-reader Layer-1 quality_criteria work:

| Step | Result |
|---|---|
| FU#61 v1 (cold-reader hand-patch) — runtime dryrun | Mostly worked; over-corrected at salutation (Greek alphabet + Egyptian transliteration both lost) |
| FU#61 v2 (head/seal vs body distinction) — drafted | Never empirically tested; superseded by v3 |
| FU#61 v3 (5-criteria reshape via Pass 4b prompt addition) — runtime dryrun | **Best version.** Greek script preserved + inline glosses + 3 additional gains (issue-not-argue mode + honest mark-of-limit + direct queenly speaker acknowledgment) |
| FU#61 v3 propagation | Pass 4b prompt addition committed `91947a7`; Cleopatra card committed `c89d186`; Dostoevsky FU#61-fresh committed `5088d67` |

### Validation gap discovery (this thread, FU#62)

While diagnosing why a dryrun re-run took 8 minutes instead of expected 1-2:
- Read `runtime/flows/voice/step1_validation.py` + orchestrator
- Discovered: validation regen-on-flag promised by spec is unimplemented in code
- Filed FU#62 with two paths (A: implement regen ~half-day; B: update spec to match implementation, ~30 min) and recommendation **B**
- Surfaced spec/impl divergence on validation parallelism (per-voice serial, not per-pair parallel; ~20-40 min wall not 3-5 min as spec claimed; ~$3-15 cost not $30-50)

### Step 3 redesign reasoning

User dropped FU#49E reviewer-pass framing on 2026-04-29 evening; this session worked through the redesign:

| Stage | Outcome |
|---|---|
| Read 5 canonical docs line-by-line | Briefing v3.1 + Frame Concept v1 + Persona Card v2 + Provocateur Pipeline + Voice Pipeline v2.1 |
| Layer 1 Encounter analysis on Step 2 artifacts | Plato dialogues clear Layer 1 standalone; Cleopatra prostagma probably fails standalone (taxonomy: form-shaped Layer 1 surface varies by voice) |
| Surfaced editor/frame layer as the missing connective tissue | 3 converging reasons: Layer 1 for hard-form voices (now reduced post-FU#61), title/subtitle generation, cross-voice contrast text |
| Three Step 3 shape options identified | A (skip) / B (metadata-only) / B+ (metadata + optional postscript) / C (artifact-mutating, current) |
| User caught footer-language temporal-honesty problem with B | "this artifact engaged Cleopatra" is false under B; artifact predates the engagement |
| **B+ recommended** (metadata + voice-written postscript) | Postscript IS the engagement, sits where it actually got written, body unchanged → Layer 1 protected. **No decision locked yet.** |

---

## Branch state at end-of-session

```
voice-pipeline-v2.1-align-revert (HEAD: 9eb0222) — pushed
  - 20+ commits ahead of main
  - shared with persona thread; both threads cohabited cleanly today
  - merge to main pending operator decision

athens-2026 (HEAD: 5088d67) — pushed
  - Cleopatra v3 quality_criteria committed (c89d186) + chat synced (54cd20a)
  - Dostoevsky FU#61-fresh committed (5088d67)
  - Untracked: cleopatra/octopus/ibn_battuta build files; backup card files

Untracked in code/_workspace/planning/:
  - FU61_DRYRUN_VERDICT_2026_04_30.md (superseded by FOLLOW_UPS.md FU#61 entries)
  - PIPELINE_DOWNSTREAM_DESIGN_2026_04_30.md (architectural reference; commit recommended)
  - STEP3_REDESIGN_2026_04_30.md (stale on shape; either revise or delete)
  - runtime/ (this session's deliverable)
```

---

## What's open / undecided at end-of-session

### Gating decisions (need operator answer to unblock implementation)

| # | Decision | Recommendation | Gates |
|---|---|---|---|
| **A1** | Step 3 shape: A (skip) / B (metadata-only) / B+ (metadata + postscript) / C (artifact-mutating) | **B+** | Step 3 prompt + parser + responded_to_graph + downstream surfaces |
| **A2** | Editor / Frame layer A-F decisions (separate pipeline pass; per-voice; AI-drafted operator-polished; voice-register-conditional strip rule; cross-voice visibility distributed; build editor first) | **All yes as drafted** | Editor layer build + Step 3 final design + microsite consumer behavior |
| **A3** | FU#62 path: A (implement regen, ~half-day) vs B (update spec, ~30 min) | **B** | Voice Pipeline doc accuracy + validation behavior on Athens Nights 2/3 |
| **A4** | Frame Concept doc revision (strip rule voice-register-conditional + add editor layer) | **Yes** (after A2 lands) | Frame Concept canonical accuracy |

**A1 and A2 are the gating pair.** A1 needs B+ vs alternatives confirmed; A2 needs the editor layer approved as the connective tissue. Once both land, downstream design + build can sequence.

### Cleanup decisions (low-stakes housekeeping)

| # | Decision | Recommendation |
|---|---|---|
| Disposition of `FU61_DRYRUN_VERDICT_2026_04_30.md` | Delete (superseded by FOLLOW_UPS.md FU#61 entries) |
| Disposition of `PIPELINE_DOWNSTREAM_DESIGN_2026_04_30.md` | Commit (architectural reference; stays valid) |
| Disposition of `STEP3_REDESIGN_2026_04_30.md` | Delete or revise after A1 (stale on shape) |
| Archive disposition for older planning docs | Move OPEN_ITEMS_VOICE_PIPELINE_2026_04_29.md + HANDOFF_VOICE_PIPELINE_DUAL_DRYRUN_2026_04_29.md + HANDOFF_DRYRUN_2026_04_29.md to `_workspace/archive/runtime_consolidation_2026_05_01/` (superseded by runtime/ docs) |

### Athens-blocking work not yet built (must land before May 7)

From `runtime/OPEN_ITEMS.md` Section B:
- B1. Editor / Frame layer (gates on A2)
- B2. Microsite (gates on A1 + A2)
- B3. Broadsheet / Edition Pipeline (gates on B1)
- B4. Substack draft pipeline pass (gates on B1)
- B5. Closing-show pipelines (Day 3 work; spec needed pre-Athens)
- B6. Day 4 goodbye spec
- B7. Render layer for non-text artifacts (Marley → Suno; Octopus → shader)
- B8. Admin console
- B9. Per-voice headline poetics

### Cross-pipeline + dryrun work (low-effort, high-priority)

From `runtime/OPEN_ITEMS.md` Section C:
- C1. researcher_flow.py drop temperature 1.0 + add display: summarized (~30 min)
- C2. provocateur_flow.py add display: summarized (~15 min)
- C2a. researcher_flow.py + provocateur_flow.py load_dotenv override=True (~5 min total)
- C3. publish_flow.py end-to-end exercise (PRE-ATHENS-MUST-DO)
- C4. Multi-night sequence dry-run (PRE-ATHENS-MUST-DO; continuity flow + Convention A never end-to-end exercised)
- C5. Multi-voice 3+ dry-run
- C6. Naming inconsistency cleanup (council_member_name → council_member → voice_name)
- C7. Title source pipeline (resolved by editor layer if A2 lands)
- C8. Voice-side artifact tuning (operator-side; per-voice judgment)

### Genuine open questions (from earlier in session)

| Q | Status |
|---|---|
| **Q1.** FU#47 voice-mode mode-switching: pre-Athens or post-Athens? | NOT DECIDED. ~2-3 days build for 4 mode variants. ~6/12 voices may need it. |
| **Q2.** Editor layer build timeline: who builds, on what timeline? | NOT DECIDED. After FU#61 nuance: scope reduced to title/subtitle + cross-voice contrast (Layer 1 carry no longer needed). |
| **Q3.** FU#23 cross-repo handoff trigger condition met (Phase L Plato shipped) | NOT DECIDED. Integration model needed: continuous merge / batch handoff / parallel-systems window. Currently *de facto* continuous merge on shared branch. |
| **Q4.** publish_flow.py end-to-end never run against real outputs | CONFIRMED through 2026-04-29; subsequent days unverified but no commits suggest a run. Pre-Athens-must-do. |
| **Q5.** Voice card buildout pace | Persona thread building Octopus, Ibn Battuta, Dostoevsky in flight. Cleopatra + Plato shipped. ~7 cards needed. Operator-side wall time. |

---

## What to do next session

In recommended priority order:

1. **Operator decisions on A1 + A2** — unblocks Step 3 implementation + editor layer build
2. **Once A1 + A2 land:** I draft `EDITOR_LAYER_DESIGN_2026_05_0X.md` (~400-500 lines, conceptual + implementation-ready)
3. **In parallel:** land C1 + C2 + C2a (cross-pipeline compliance, ~50 min total)
4. **C3 (publish_flow.py end-to-end exercise)** — pre-Athens-must-do; needs either re-running dev_msc_test through Researcher + Provocateur + voice_flow + publish_flow, OR hand-authored fixtures matching briefing schema
5. **C4 (multi-night sequence dry-run)** — pre-Athens-must-do; continuity flow + Convention A never end-to-end exercised
6. **A3 (FU#62 path)** — ~30 min to update Voice Pipeline doc per recommendation B
7. **A4 (Frame Concept doc revision)** — after A2

In parallel on operator side:
- Voice cards buildout (Octopus + 7 voices' DR sessions; persona thread owns)
- Cleanup decisions per "Cleanup decisions" table above

Post-Athens: A1, A2, A3, A4, C1-C8 completion + FU#42 split-card + FU#54 + Athens runtime hardening from observed behavior.

---

## Reading order for next-session pickup

1. **This doc** — for state-of-the-moment
2. **`_workspace/planning/runtime/ONBOARDING.md`** — durable runtime scaffolding
3. **`_workspace/planning/runtime/OPEN_ITEMS.md`** — authoritative open items
4. **`_workspace/planning/PIPELINE_DOWNSTREAM_DESIGN_2026_04_30.md`** — architectural reasoning for editor layer + downstream pipeline (currently uncommitted)
5. `_workspace/planning/FOLLOW_UPS.md` — for FU#23, 24, 25, 26, 27, 42, 47, 49B/E/F/M, 54, 57, 60, 61, 62 (cross-reference; runtime items in OPEN_ITEMS already)
6. `docs/AI_Assembly_Voice_Pipeline.md` — runtime contract for Voice Pipeline (v2.1, may need Step 3 section rewrite per A1)
7. `docs/AI_Assembly_Briefing_v3_1.md` lines 91, 167, 177–181, 232–238, 292 — briefing's Step 3 framing
8. `docs/AI_Assembly_Frame_Concept_v1.md` — frame layer (currently stale on FU#61 + editor layer; revision pending A2 + A4)

If task is Step 3 implementation: read this doc + ONBOARDING + OPEN_ITEMS A1 + PIPELINE_DOWNSTREAM_DESIGN.

If task is editor layer build: read this doc + OPEN_ITEMS A2 + PIPELINE_DOWNSTREAM_DESIGN + Frame Concept v1 + persona-card per-voice headline-poetics work (FU#61 v3 commits as reference).

If task is C1/C2/C2a cross-pipeline compliance: read FU#60 in FOLLOW_UPS + `runtime/flows/voice/_anthropic_call.py` for the pattern + change `runtime/flows/researcher_flow.py` + `runtime/flows/provocateur_flow.py` accordingly.

If task is publish_flow.py end-to-end (C3): read `runtime/flows/publish_flow.py` + dev_msc_test directory at `~/Desktop/AI Assembly/archive/runs/runtime/dev_msc_test/` + decide between re-run-upstream OR hand-author-fixtures path.

---

## Calibration carried forward

- **Operator preference is direct.** When pushed back, re-investigate before bulldozing.
- **Provotype framing is load-bearing.** This is a civic-design artifact for WBBF Athens 2026. Visible construction is a feature.
- **Voice Pipeline first, chat-test as instrument.** Voice Pipeline (runtime) consumes specific card fields via API per-step; that's the production target.
- **Athens needs full runtime stack ready, not just cards.** Operator priority.
- **Defaults to strips, defer additions, validate empirically.** Per 04-28 revert lesson.
- **Always RENAME, don't DELETE for cache busting.** Per 04-29 dryrun #1 loss lesson.
- **Always `--skip-validation` for dryrun work.** Per 2026-04-30 finding (validation re-runs against cached Step 1 outputs; no regen mechanism; 8 min/voice burned for nothing).
- **Card-as-system-prompt is the production pattern at runtime.** Per 04-29 architectural ratification.

---

## Cumulative cost this session (this thread only)

Rough estimate (operator-paid):
- FU#61 v1 dryrun (Step 1 validation + Step 2 cached → cache-bust + Step 2 only): ~$2 (validation 8 min + Step 2 ~70s)
- FU#61 v3 dryrun (Step 2 only with --skip-validation): ~$1 (~80s)
- Planning doc reads + writes: ~$0 (documentation work)
- **Total: ~$3** (this thread)

Total session cost across both threads: see HANDOFF_2026_04_29_LATE for persona-thread costs (~$140 over the prior days; today's persona thread costs lower since most work was prompt addition + surgical patches).

---

## Tests + branch hygiene

- Tests: 223/223 passing throughout
- Branch state: `voice-pipeline-v2.1-align-revert` 20+ commits ahead of main; no conflicts; pushed
- `_workspace/planning/runtime/` subfolder is the new authoritative truth for runtime work going forward
- Cleanup of superseded docs in `_workspace/planning/` pending operator approval

---

## Cross-thread coordination resolved (end-of-session)

Memo to persona/voices thread (about freeze + restructure) sent + acknowledged.
Persona-thread response landed `ab62fe1` and confirmed:

- **Freeze convention agreed** — no pushback. Voices/OPEN_ITEMS §4 already
  organizes by topic, not FU#; FOLLOW_UPS as historical ledger holds.
- **Cross-references in voices/ONBOARDING landed** — pointing at parent
  ONBOARDING's cross-cutting DON'Ts + calibration. Symmetric to runtime/
  ONBOARDING.md cross-reference (commit `7311467`).
- **Voice-build OPEN_ITEMS coverage verified complete** — FU#33/39/56 all
  present; FU#19/33 P1/43/44/50(2) backfilled (commit `143b89c`).
- **Parent ONBOARDING small-fix landed** — line 95-96 reflected that the 6
  dated handoffs + BRIEF_OPUS were already archived (commit `82e5a65`).
  Parent ONBOARDING's §"What this folder root contains" updated.
- **Athens-2026 modified files clarified by persona thread:**
  - `voices/cleopatra/07_persona_card_assembled.json` post-`c89d186` — trailing
    newline difference (cosmetic; pending operator decision per voices/HANDOFF §60)
  - `archive/first_run/voices/cleopatra/01_research/03_dr_prompts/08_section_6_dr_prompt.md`
    — sed-artifact from 04-27 (also pending operator decision per voices/HANDOFF §60)

Both threads now symmetric on convention + structure. No outstanding cross-thread
items at session end.

## Late-session reorganization work (post-cross-thread-sync)

After the FU#61 + FU#62 + Step 3 substantive work + cross-thread coordination
resolved, the session continued with hygiene + reorganization. Six additional
commits landed:

- **`f56cd09`** — `_workspace/planning/ONBOARDING.md` slimmed from 412 → 104
  lines (thin index + cross-cutting DON'Ts/calibration; persona-architecture
  content removed since it lives in `voices/ONBOARDING.md`).
- **`0f59075`** — relocated `_workspace/arch_03_baseline_snapshot/` (1.5 MB) +
  `_workspace/sentinel_baselines/` (240 KB) from code repo to umbrella
  `~/Desktop/AI Assembly/archive/`. Both gitignored; relocation removes ~1.7 MB
  of clutter from `code/_workspace/`. Same pattern applied to umbrella-level
  `_workspace_safety_snapshots/` (684 KB, the 2026-04-28 prompt-revert safety
  snapshot) — relocated to `archive/personas_prompts_PRE_582af96_REVERT_*/`.
- **`6d0e2e2`** — **FOLLOW_UPS.md FROZEN** as historical ledger of FU#1–62.
  No new FU# numbering going forward. Tracking convention documented in
  `code/CLAUDE.md` §"Planning / tracking conventions" (~50 lines).
  Subfolder OPEN_ITEMS is now the single home for new items.
- **`b6de5ef`** — `code/CLAUDE.md` Voice Pipeline spec reference fixed
  (was claiming Step 3 unspecified + FU#49E post-Athens; corrected to v2.1
  + Step 3 redesign in flight per `runtime/OPEN_ITEMS.md` A1).
- **`f0eaa95`** — `research/` (176 KB, 5 compass artifacts + READMEs)
  relocated from top-level to `docs/research/`. Top-level repo categories:
  5 → 4 (`docs/` + `runtime/` + `personas/` + `_workspace/`). Updated
  references in CLAUDE.md, README.md, docs/CURRENT_STATE.md, docs/README.md,
  docs/references.md.
- **`3b576d2`** — `projects/` reorganized to two top-level entries only:
  `current-tests/` (sandbox + voice-pipeline-dryrun + runs/dev_msc_test inside)
  and `athens-2026/` (production). Phase L projects (phase-l-plato + phase-l-
  dostoevsky) relocated to umbrella `archive/`. dev_msc_test relocated from
  `archive/runs/runtime/` to `projects/current-tests/runs/dev_msc_test/`
  (re-classified from frozen historical run to active test reference).
  `.env`, 8 doc + code reference updates.

Plus on the athens-2026 data repo (separate private git):
- **`0fb2332`** — `archive/first_run/` (4.5 MB; 177 files; first-run voice
  generation from 2026-04-27) relocated to umbrella
  `~/Desktop/AI Assembly/archive/athens-2026/first_run/`. Side-effect: resolved
  the long-pending sed-artifact at `archive/first_run/voices/cleopatra/...
  /08_section_6_dr_prompt.md` (file now outside git tracking).

**Net effect of late-session work:**
- Single-source-of-truth for tracking established (subfolder OPEN_ITEMS only;
  FOLLOW_UPS.md frozen)
- Two-tier archive convention consistently applied: in-repo
  `code/_workspace/archive/` for text docs; umbrella `~/Desktop/AI Assembly/archive/`
  for run data + dev artifacts + dormant working projects
- `projects/` reduced to active-only (current-tests + athens-2026)
- `code/_workspace/` no longer holds dev snapshots; cleaner repo
- Documentation (CLAUDE.md, planning/ONBOARDING.md) reflects the new
  convention canonically — both threads aligned

**Still outstanding from earlier in session** (not changed by reorganization):
A1 (Step 3 shape), A2 (Editor / Frame layer), A3 (FU#62 path), Q1-Q5 in
"What's open / undecided at end-of-session" above.

**Athens-2026 working tree** still has one pending file:
`voices/cleopatra/07_persona_card_assembled.json` (trailing-newline difference;
pending operator decision per voices/HANDOFF §60). Unchanged from session start.

## Closing notes

This session was the runtime-side companion to the persona thread's FU#61 v3 work. Net architectural change for runtime: a clean separation of authoritative truth (runtime/ subfolder) from session-state artifacts (dated handoffs); a comprehensive open-items audit against all source material; and the Step 3 redesign work surfaced + reasoned through but not landed (gating on A1 decision).

The pipeline downstream of Step 2 is now architecturally clearer (editor layer as the missing connective tissue) but UNBUILT. Six days to Athens. The gating decisions (A1 + A2) need operator answers to unblock the build.

---

## Phase 2 late-session: A1+A2+A3 decisions + legitimacy test + bugfixes (commits `d4cea03`..`ee824be`)

After this handoff was first written, the session continued with a substantial second phase. Operator landed all 3 architecture-side gating decisions, ran a comprehensive runtime test, and the test surfaced two Athens-blocking bugs that have been fixed.

### Architectural decisions resolved

- **A1 (Step 3 shape) ✅ Option A — skipped for Athens.** Pipeline runs Steps 1+2 only via `--skip-step3`. Cross-voice visibility moves to editor layer + Substack. Module dormant; ~2-day re-add path documented for post-editor/microsite. Voice Pipeline doc updated.
- **A2 (Editor / Frame layer) ✅ A+B+C+F decided; D deferred; E elsewhere.** Per-theme editor architecture (not per-voice editorial chrome); all-AI drafting (no operator polish phase); voice artifacts ship as-is with Provocateur formulation as micro-header; parallel build with microsite specifying editor output schema.
- **A3 (FU#62 path) ✅ B — spec updated to diagnostic-only.** Athens validation policy: Night 1 ON / Nights 2+3 OFF.

### Pre-Athens infrastructure landed

- **C1 + C2 + C2a:** researcher + provocateur + transcription brought into FU#60 compliance (drop temperature, add display:summarized, load_dotenv override=True). Commit `d4cea03`.
- **C6:** closed as misdiagnosed — three names mark intentional domain boundaries.
- **C11:** speaker bios populated from WBBF program HTML (217/224 active, 96.9%). Built `populate_speakers_from_program_html.py` + handles operator decisions (Path B for Dave + Helen Edwards). Commit on athens-2026 `4ad86df`.
- **C11/recording schedule:** 25 sessions across 3 nights flagged via `apply_ai_assembly_flags_from_csv.py` from operator-supplied authoritative CSV. Same commit.

### Athens-blocking bugs found + fixed

**Bug 1 — Continuity-after-Step-2** (commit `ccc6229`). At `voice_flow.py:458` the continuity gate had `not skip_step3` baked in, silently disabling continuity when Athens production CLI used `--skip-step3` (per A1). Athens Nights 2+3 would have launched with no continuity carried forward; voices would have written as if prior nights didn't happen. Fixed: continuity now fires whenever Step 2 completes. **Verified end-to-end by Test 1 (3-night sequence).**

**Bug 2 — thinking_tokens always 0** (commit `8c47e1f`). Six places across both pipelines read `getattr(usage, "thinking_tokens", 0)` — but Anthropic SDK's Usage object has no thinking_tokens field at any version (verified by docs + live SDK probe + LiteLLM cross-check). Fixed by computing via subtraction: `output_tokens - count_tokens(response)` using the free `count_tokens` API. Applied to Voice Pipeline (`_anthropic_call.py` returns 4-tuple) + Persona Pipeline (`clients.py` `call_claude` populates `usage["thinking_tokens"]`). Verified live in Test 2 v2 (values 2286-4828 across 4 voices).

### The legitimacy test (operator-driven runtime confidence check)

Operator authored 3 mock formulations on "The Legitimacy of the Invisible" (algorithmic governance / when no one looks at you / withdrawal from public life). Two tests run on the 4 shipped voices (Plato, Cleopatra, Dostoevsky, Battuta):

- **Test 1: 3-night sequence** (1 formulation per night). Verified continuity-after-Step-2 bugfix end-to-end. Cross-night memory observed (Dostoevsky N3: *"I wrote that pause yesterday. I will not write it again"*). Voice register fidelity preserved across all 3 nights.
- **Test 2: single night with all 3 formulations per voice.** All 4 voices chose `focus_decision: "woven across all three"` — synthesis branch verified. Each voice articulated a unifying through-line in their own grammar.
- **Test 2 v2:** re-run with distinct theme_display_titles to remove the title-shape confound. **Still wove on all 4 voices.** Diagnosis: the synthesis bias goes deeper than title shape — voices with rich conceptual machinery default to synthesis when content converges.

Total spend: ~$50-80 API + ~25 min wall across all three runs.

Test artifacts: `projects/current-tests/voice-pipeline-dryrun/runs/legitimacy_test*/`. Test report: `voice-pipeline-dryrun/legitimacy_test_report.md` (~423K markdown, 2425 lines, 39 collapsible thinking traces).

### External review findings (5 concerns + patterns)

External reviewer raised 5 substantive concerns. After investigation:

- **N3 continuity verification:** ✅ renderer bug, not regression. Field name `continuity_block_if_night_3` (not `_if_night_2`). Fixed report renderer.
- **Test 2 confound:** Test 2 v2 attempted partial fix; still wove. Test 3 with truly-divergent topics still recommended (C18).
- **C15 (consumed_detailed_responses lineage gap):** ✅ misdiagnosed; field IS populated under `artifact.lineage.consumed_detailed_responses`. Lineage is auditable from artifact alone.
- **C19 token economics audit ✅ completed.** Finding: 85-88% of Step 2 input is the persona card (system prompt). Filed C19a — Anthropic prompt caching is not enabled in runtime. With caching: ~87% savings on cached portion across reads 2-N per voice per night. Athens 3-night savings: ~$60-75 input. Pre-Athens-eligible.
- **C20 voice recurrence patterns:** Plato Theuth/Thamus, Battuta Tughluq beard-plucking, Dostoevsky verbal tic on closing-on-suspended-judgment + operator-discovered Plato Socrates-death anachronism (sharp finding — Socrates as first-person narrator referencing his own death; would be caught by Pass 7-anachronism on Night 1, but Athens N2+3 has validation OFF).
  Memo for persona thread: `_workspace/planning/voices/MEMO_2026_05_01_recurrence_patterns_from_legitimacy_test.md`.

### State of architecture-side gates

All 3 closed:
- A1 ✅ Option A (Step 3 skipped for Athens)
- A2 ✅ A+B+C+F decided
- A3 ✅ Path B (validation diagnostic-only)

**Only blocker for editor build is microsite design** (operator-side, drives editor output schema per A2 F). Pre-Athens-must-do runtime items remaining: C9 (Provocateur Night 2/3 exclusion filter), C19a (prompt caching, optional), Test 3 (focus-on-one branch verification).

### Net pipeline state at end of session

- All FU#60 thinking-observability is now real (was silently broken; fixed)
- Continuity flow is correct under A1's `--skip-step3` Athens production CLI (was silently broken; fixed)
- Voice register fidelity verified empirically across 3 nights and multi-formulation synthesis
- Cross-night memory verified (continuity overlay loads on N2+; voices reference prior nights)
- One voice-card-side anachronism identified pre-Athens (Plato Socrates-death) for persona-thread resolution

### Reading order for next-session pickup (revised)

1. This doc (state of moment)
2. `_workspace/planning/runtime/OPEN_ITEMS.md` — A1/A2/A3 resolved; C-section status current; C19a + C18 + C20 outstanding
3. `_workspace/planning/voices/MEMO_2026_05_01_recurrence_patterns_from_legitimacy_test.md` — for persona thread
4. `projects/current-tests/voice-pipeline-dryrun/legitimacy_test_report.md` — full test evidence (operator-facing + diagnostic)
5. `docs/AI_Assembly_Voice_Pipeline.md` — updated for A1/A2/A3 (Athens production CLI documented)

### Cumulative cost — full session

Rough estimate (operator-paid):
- FU#61 v1 + v3 dryruns (Phase 1, earlier today): ~$3
- Legitimacy Test 1 (3 nights × 4 voices): ~$30-40
- Legitimacy Test 2 (single night, 4×3 formulations): ~$15
- Legitimacy Test 2 v2 (re-run with divergent titles): ~$15
- Other API calls (count_tokens audit, etc.): ~$1
- **Total session API: ~$65-75**

Engineering wall time on bugfixes + audit + report: ~3-4 hr human-equivalent.

*End of HANDOFF_2026_05_01.md (Phase 2 late-session addendum).*
