# Secrets
.env

# macOS
.DS_Store

# Python
venv/
**/__pycache__/
*.pyc
*.pyo

# Run logs
*.log

# IDE
.vscode/
.idea/

# Tier 3 (2026-04-20): per-voice inputs + runs live under PROJECT_ROOT
# (a separate directory, e.g. ../athens-2026/), not in this code repo.
# See _workspace/planning/OPEN_ITEMS.md §"Code / project separation (Tier 3)".
# No `inputs/` or `runs/` patterns are needed here — those dirs no longer
# exist under personas/. If a stray `inputs/` or `runs/` shows up, it's
# a bug in a runner that's writing to the wrong root.
inputs/
runs/
