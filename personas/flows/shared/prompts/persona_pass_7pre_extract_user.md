PERSONA CARD TO EXTRACT CLAIMS FROM:
{{ persona_card_json }}

Extract the claim items per the system prompt. Do NOT verify — the
verification stage runs next with primary-text access. Output the JSON
object as specified.
