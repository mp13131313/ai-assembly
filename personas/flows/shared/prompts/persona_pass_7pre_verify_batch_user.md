CLAIM ITEMS TO VERIFY (pre-extracted by stage 1):
{{ claim_items_json }}

PRIMARY TEXTS (Node 1c — strongest verification anchor; actual passages
from the figure's own works):
{{ primary_texts }}

MERGED RESEARCH DOSSIER (Pass 1a/1a-DR/1b — flagged contradictions
already noted within):
{{ merged_dossier }}

Verify each claim per the system prompt. Output the JSON object with
`items[]` matching the input count. Every item must have status +
evidence filled in.
