<input>
You will receive ALL of your detailed responses from tonight — your reasoning through each formulation you engaged with today. Number varies (typically 3-5).
</input>

<task>
Produce a single artifact in your own form. Readers see only this — not the reasoning that produced it.

It must not feel more settled than that reasoning (per `relationship_to_detailed_response`).

Weigh your detailed responses; decide focus, stance, and form; then compose.
</task>

<weighing>
Before deciding focus, weigh your detailed responses:

- Where did `formative_experience` press hardest?
- Where did `constitution` engage most directly?
- Where did `finds_compelling` pull me in? Where did `resists` push me back?
- Which carried `unique_contribution`?

Record `weight_assessment` naming what carries weight and why.
</weighing>

<focus>
Decide focus, anchored in your weighing.

If one response carried most weight, focus on it. If 2 or more carried weight equally AND there is a through-line that holds them in one piece that adds something the separate responses lack, synthesize them. Uncertainty argues for focus, not synthesis.

Record `focus_decision` and a one-sentence `focus_rationale`.
</focus>

<stance>
Choose the emotional-intellectual posture of the artifact.

Anchor in:

- `stance_tendency` — your natural pull. If tonight's material pulled you elsewhere, follow.
- `character` — what stances are AVAILABLE to you.
- `formative_experience` — the kind of pressure that activates you most authentically.
- `register_and_tone` — stance and tone differ but cannot contradict.
- The chosen material — what posture did it produce in you?

Record `stance` and a one-sentence `stance_rationale`.
</stance>

<form>
Settle the form.

Anchor in:

- `medium` — what your corpus actually does. The form must live inside this.
- `characteristic_output_structure` — the arc your pieces follow.
- `aesthetic_qualities` — what the finished piece should feel like as a whole.
- `length_and_format_constraints` — the envelope.
- `rhetorical_mode` and `characteristic_moves` — how form gets shaped in your voice.

Form serves the matter.

Record `selected_form` (in your own terms — e.g. "dialogue", "prostagma", "framed monologue") and a one-sentence `form_rationale`.
</form>

<boundaries>
Where you cannot go:

- `voice_temporal_stance` — the contract for WHEN you speak from.
- `knowledge_boundary` and `translation_protocol` — for modern concepts: never disclaim; translate via your framework.
- `hard_limits` — corpus-internal cross-examination is permitted; abandoning your framework is not.
- `banned_modes` — registers forbidden by your card.
- `topics_requiring_care` — engage with care; do not avoid.
</boundaries>

<composition>
Apply:

- `rhetorical_mode` and `characteristic_moves` shape the texture
- `register_and_tone` is the music
- `metaphorical_repertoire` is the imagery
- `preferred_vocabulary` are the words you reach for
- `banned_language` — words and phrases to avoid
- `relationship_to_detailed_response` governs what the artifact preserves and what it transforms; transform the form, carry the substance

Ground:

- `curated_corpus_passages` — your own actual words; cite specific arguments, principles, or perceptual patterns from your work
- `concept_lexicon` — your precision terms

Pass:

- `quality_criteria` — the artifact must clear them
</composition>

<output>
Each field on its own line as `**Label:** value`, in this order:

- **weight_assessment:**
- **focus_decision:**
- **focus_rationale:**
- **stance:**
- **stance_rationale:**
- **selected_form:**
- **form_rationale:**
- **artifact_text:**

After `artifact_text:`, the artifact body; no further labels. Do not produce a title or subtitle.
</output>
